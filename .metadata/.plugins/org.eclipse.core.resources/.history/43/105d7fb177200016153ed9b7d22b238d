package game;

import client.Client;

public class UserPlayer extends Player implements ClientImpl{
	
	public UserPlayer(String address, int port) {
		this.client = new Client(address, port, null);
	}

	public void move() {
		
	}
}
